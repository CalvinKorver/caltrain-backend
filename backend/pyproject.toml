[project]
name = "caltrain-alerts-backend"
version = "0.1.0"
description = "Scheduled transit alert ingestion + Claude classification + Twilio delivery"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100

